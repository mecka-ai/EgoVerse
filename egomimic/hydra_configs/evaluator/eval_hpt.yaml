_target_: egomimic.eval.eval_hpt.HPTEvalVideo
# Step-based viz gating (see eval_pi.yaml). Must be a multiple of the run's
# val_check_interval for the modulo to line up with validation passes.
viz_every_n_steps: 10000
